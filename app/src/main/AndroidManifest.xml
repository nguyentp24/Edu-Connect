<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EduConnect"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        tools:targetApi="31">

        <!-- LAUNCHER -->
        <activity
            android:name="com.example.educonnect.ui.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Main -->
        <activity
            android:name="com.example.educonnect.ui.main.MainActivity"
            android:exported="false" />

        <!-- Attendance -->
        <activity
            android:name="com.example.educonnect.ui.attendance.AttendanceActivity"
            android:exported="false"
            android:theme="@style/Theme.EduConnect" />

        <activity
            android:name="com.example.educonnect.ui.attendance.StudentEvaluationActivity"
            android:exported="false"
            android:theme="@style/Theme.EduConnect"/>

        <activity
            android:name="com.example.educonnect.ui.profile.ClassListActivity"
            android:exported="false"
            android:theme="@style/Theme.EduConnect"/>

    </application>
</manifest>
